# Runbook — Deploy Worker

Stub. Filled in at Phase 0.A.

## Quick steps (target)

```bash
cd apps/worker
wrangler whoami            # confirm logged in
wrangler deploy            # deploys to production
```

## Rollback

`wrangler rollback` to a previous deployment ID.
