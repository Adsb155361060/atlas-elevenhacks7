# direnv: auto-load .env.local on cd into this repo.
# Run `direnv allow` once after cloning.

dotenv_if_exists .env.local
dotenv_if_exists .env.${USER}.local

# Per-workspace overrides (optional)
PATH_add ./scripts
