#!/usr/bin/env bash
# Atlas latency trace — reads the most recent voice-turn from structured logs and
# prints per-stage timings: wake → audio_open → stt_first_partial → stt_final →
# llm_first_token → llm_complete → tts_first_audio → playback_start.
# Stub until §3.4 lands.

set -euo pipefail

echo "latency-trace.sh — placeholder. Will be filled in at Phase 0.E once tracing is wired."
echo "Until then: no traces to read."
exit 0
