#!/usr/bin/env bash
# Atlas end-to-end smoke test.
# Stub until Phase 0.A lands (Worker + voice loop).
# Once filled in, this should: start Worker locally, fire a test request via the
# desktop's WebSocket path or a curl proxy, assert a Flash v2.5 audio reply arrives
# with <1s perceived latency, exit non-zero on regression.

set -euo pipefail

echo "smoke.sh — placeholder. Will be filled in at Phase 0.E (voice loop end-to-end)."
echo "Until then: nothing to verify."
exit 0
